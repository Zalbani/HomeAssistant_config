type: conditional
conditions:
  - condition: state
    entity: binary_sensor.pc_online
    state: "on"
chip:
  type: template
  entity: switch.alban_pc_fixe
  icon: mdi:desktop-tower
  icon_color: blue
  content: "{{ states('sensor.pc_cpuload') | round(0) }}%"
  tap_action:
    action: navigate
    navigation_path: /overview-dashboard/office
